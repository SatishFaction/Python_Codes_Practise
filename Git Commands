git init
git remote rm origin
git remote add origin git@github.com:username/myapp.git
git pull origin master
git status--(to check what files are added in the index)
untracked files are those which are not added in the index
#commiting changes in ex file 
git add ex.txt
git add -A adding all files to index
Set your username:
git config --global user.name "FIRST_NAME LAST_NAME"
Set your email address:
git config --global user.email "MY_NAME@example.com"

git config --list #Listing configuration have to be in the root directory
git log
git checkout master #move from one branch to another
git branch firstBranch # to create a branch
git merge firstBranch

echo "# Python_Codes_Practise" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/SatishFaction/Python_Codes_Practise.git
git push -u origin master
git clone https://github.com/SatishFaction/Sql_Class_Scripts.git
git commit -m "comiiting changes"
